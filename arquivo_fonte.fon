start:
    int idade;

    
    screen("Digite sua idade");	    
    read(idade);

    if idade > 18 & idade <60:
        screen("Voce e adulto");
    else:
        if idade<18:
            screen("Voce e adolecente ou crianca");
        else:
            screen("Você e idoso");
        :endif;
    :endif;      

    
:end